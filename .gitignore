node_modules
*.log
.DS_Store
.next
.vercel
.turbo
.tsbuildinfo

# Playwright artifacts — only the baselines under tests/__screenshots__/
# are committed; runtime reports + diff outputs stay local.
playwright-report/
test-results/
blob-report/
playwright/.cache/

# macOS Spotlight dupe defense — see SAAS Factory memory reference_repo_hygiene_patterns
**/* [0-9].*
